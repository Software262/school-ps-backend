[project]
name = "backend"
version = "0.1.0"
description = "Backend for the school peace and safety project"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[all]>=0.136.1",
]
